#include <iostream> //외부 라이브러리를 불러옴
#include <vector>
#include <string>
#include <fstream>

using namespace std; // std namespace를 기본으로 사용

int main(){ // 기본으로 int main 사용
    cout << "Hello World" << endl;
    
    return 0;
}

